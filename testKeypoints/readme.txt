The testKeypoints.py script can only test one camera at a time. 
Make sure that all the images are in the images folder
Rename the annotation you are using to annotation.json
The output will be in the out folder. 